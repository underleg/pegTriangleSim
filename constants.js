
const xsize = 700;
const ysize = 700;

const frozenLimit = 100;

const radius = 15
const ballScale = radius / 12.0;
const diameter = radius * 2;
const cos30 = 0.86602540378;

const startY = -10;

const ballGap = 1;

const wallXSize = 20;

const  randomsDirectionsPerBall = 10;

const fontStyle = new PIXI.TextStyle({
    fontFamily: 'Arial',
    align: "center",
    fontSize: 10,
    fontStyle: 'italic',
    fontWeight: 'bold',
    fill: ['#000000'], 
});
